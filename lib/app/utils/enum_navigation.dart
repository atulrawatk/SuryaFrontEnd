enum Pin { createPin, confirmPin, enterPin }
enum ContactList {
  chatContact,
  callContact,
  forwardMessageContact,
}

enum AppBarchange {
  normal,
  search,
  selectcontacts,
}

enum MediaType{
  video,
  image,
  document,
  audio,
  none
}

enum MessageType{
  text,
  media
}
